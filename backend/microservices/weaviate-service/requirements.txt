# Weaviate Service - Multi-Agent RAG Pipeline
# Primary LLM: vLLM (high-throughput GPU inference)
# Models: Qwen3-VL-4B-Instruct + Qwen3-VL-Embedding-2B + Qwen3-VL-Reranker-2B
# Framework: Qwen-Agent (optimized for Qwen models)

# Weaviate vector database
weaviate-client>=4.9.0

# MCP (Model Context Protocol) - External data source integration
# Enables connecting to storage, databases, REST APIs via MCP servers
# Documentation: https://modelcontextprotocol.io/
mcp>=1.8.0
pyyaml>=6.0.0

# FastAPI and core
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0
pydantic-settings>=2.1.0

# HTTP clients
httpx>=0.25.0
aiohttp>=3.9.0

# Vector and ML - Cross-encoder reranking
sentence-transformers>=2.2.2
torch>=1.11.0
numpy>=1.24.0

# NLP for Structural Intelligence Layer (SIL)
# spaCy: Lemmatization, NER, POS tagging
# Install models after: python -m spacy download es_core_news_md && python -m spacy download en_core_web_md
spacy>=3.7.0

# rapidfuzz: Fast fuzzy string matching for typo tolerance
rapidfuzz>=3.5.0

# transformers: Zero-shot classification
# Note: Uses MiniLM-based models for speed (not full LLMs)
transformers>=4.36.0

# Semantic Router - Fast intent classification via embeddings
# Use base package (no [local]) to avoid llama-cpp build dependency
semantic-router>=0.0.50

# FastEmbed - ONNX-optimized embeddings for ultra-low latency (~1-3ms vs ~10ms)
# Uses quantized models and ONNX Runtime for 10-15x speedup over sentence-transformers
fastembed>=0.4.0

# SetFit - Few-shot fine-tuning for intent classification
# NexusRouter uses SetFit for trainable ML-based intent classification
# Requires minimal training data (8-16 examples per class)
# Documentation: https://github.com/huggingface/setfit
setfit>=1.0.0
datasets>=2.14.0  # HuggingFace datasets for SetFit training
scikit-learn>=1.3.0  # Required for metrics (F1 score, etc.)

# Database and storage
asyncpg>=0.29.0
redis>=5.0.0

# PDF manipulation
PyMuPDF>=1.24.0  # For PDF annotation service
pymupdf4llm>=0.0.17  # For converting PDFs to LLM-optimized Markdown
python-multipart>=0.0.9  # For Form data handling in FastAPI

# Logging and monitoring
structlog>=23.2.0
loguru>=0.7.0

# Qwen-Agent Framework
# Multi-agent orchestration optimized for Qwen models (Qwen3-VL)
# Features: Function calling, MCP support, Code Interpreter, RAG
# Documentation: https://github.com/QwenLM/Qwen-Agent
# Handles tool parsing internally (no vLLM --tool-call-parser needed)
qwen-agent>=0.0.14
python-dateutil>=2.8.0  # Required by qwen-agent for DateRuntime

# OpenAI client for vLLM compatibility layer
openai>=1.0.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
