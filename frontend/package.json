{
  "name": "nexusdocs360",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:emma": "turbo dev --filter=@nexus/emma",
    "dev:saas": "turbo dev --filter=@nexus/saas",
    "build": "turbo build",
    "build:emma": "turbo build --filter=@nexus/emma",
    "build:saas": "turbo build --filter=@nexus/saas",
    "lint": "turbo lint",
    "clean": "turbo clean && rm -rf node_modules",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.0",
    "typescript": "^5"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
