/**
 * Type stub for @/lib/i18n/hooks.
 * Actual implementation lives in the consuming app.
 */
export declare function useTranslation(): {
  t: (key: string, params?: Record<string, string>) => string
  locale: string
}
